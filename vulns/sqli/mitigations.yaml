order_of_preference:
  - id: parameterized-queries
    snippet:
      js: "db.query('SELECT * FROM users WHERE id = ?', [id])"
      python: "cur.execute('SELECT * FROM users WHERE id = ?', (id,))"
      php: "$stmt=$pdo->prepare('SELECT * FROM users WHERE id=?');$stmt->execute([$id]);"
explanations:
  parameterized-queries: Use bound parameters.
